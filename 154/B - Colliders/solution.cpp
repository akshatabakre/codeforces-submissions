/*Template Author - Akshata*/#include<bits/stdc++.h>using namespace std;#define int long long const int maxN = 1000001;int spf[maxN]; void pre(){for(int i=0;i<maxN;i++)spf[i]=i;for(int i=2;i*i<maxN;i++)if(spf[i]==i)for(int j=i*i;j<maxN;j+=i)if(spf[j]==j)spf[j]=i;}void inparr(vector<int>&a,int n){for(int i=0;i<n;i++)cin>>a[i];} void solve(){    int n,m;    cin>>n>>m;    unordered_map<int,int> on,primetonum;    while(m--){        char ch;        int num;        cin>>ch>>num;        if(ch=='-'){            if(on[num]==1){                on[num]=0;                while(num!=1){                    primetonum[spf[num]] = 0;                    num/=spf[num];                }                cout<<"Success\n";            }else{                cout<<"Already off\n";            }        }else{            if(on[num]==1){                cout<<"Already on"<<endl;            }else{                int x = num;                bool valid = true;                while(x!=1){                    if(primetonum[spf[x]]!=0){                        cout<<"Conflict with "<<primetonum[spf[x]]<<endl;                        valid = false;                        break;                    }                    x/=spf[x];                }                if(valid){                    x = num;                    while(x!=1){                        primetonum[spf[x]] = num;                        x/=spf[x];                    }                    on[num] = 1;                    cout<<"Success"<<endl;                }            }        }    }} int32_t main(){ios_base::sync_with_stdio(false);cin.tie(NULL);pre(); // Uncomment for Sieveint t=1;while(t--)solve();return 0;}