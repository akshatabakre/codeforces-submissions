/*Template Author - Akshata*/#include<bits/stdc++.h>using namespace std;#define int long long const int maxN = 1000001;int spf[maxN]; void pre(){for(int i=0;i<maxN;i++)spf[i]=i;for(int i=2;i*i<maxN;i++)if(spf[i]==i)for(int j=i*i;j<maxN;j+=i)if(spf[j]==j)spf[j]=i;}void inparr(vector<int>&a,int n){for(int i=0;i<n;i++)cin>>a[i];} void solve(){    int n;    cin>>n;    vector<int> v(n);    for(int i=0;i<n;i++){        cin>>v[i];    }    vector<int> res(n+1,-1);    for(int i:v){        res[i] = 1;    }    for(int i=1;i<=n;i++){        if(res[i] == -1){            int minnums = 1e9;            for(int j=2;j*j<=i;j++){                if(i%j==0){                    if(res[j]!=-1 && res[i/j]!=-1){                        minnums = min(res[j]+res[i/j],minnums);                    }                }            }            if(minnums<1e9){                res[i] = minnums;            }        }    }    for(int i=1;i<=n;i++){        cout<<res[i]<<" ";    }    cout<<endl;} int32_t main(){ios_base::sync_with_stdio(false);cin.tie(NULL);// pre(); // Uncomment for Sieveint t=1;cin>>t;while(t--)solve();return 0;}